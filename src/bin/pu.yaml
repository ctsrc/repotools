name: pu
version: 1.0.0
about: push commits or tags to remote git repository
args:
  - push_tags:
      long: tags
      help: Push tags.
  - force_push:
      short: f
      help: Force push.
  - repository:
      help: Named remote or URL to push to.
